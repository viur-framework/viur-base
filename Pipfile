[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
viur-core = "~=3.8"

[dev-packages]
viur-cli = "~=2.3"
pycodestyle = "~=2.10"

[requires]
python_version = "3.13"

[scripts]
kill_server = "bash -c 'pkill app_server -ef -9 ; pkill gunicorn -ef -9'"
