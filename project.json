{
    "cli-version": "2.0.1",
    "default": {
        "application_name": "{{app_id}}-dev",
        "builds": {
            "admin": {
                "command": "viur package install admin",
                "kind": "exec",
                "version": "4.2.6"
            },
            "npm": {
                "command": "build",
                "kind": "npm",
                "source": ""
            }
        },
        "distribution_folder": "./deploy",
        "sources_folder": "./sources",
        "version": "$(year)-$(month)-$(day)-$(user)"
    },
    "format": "2.0.0",
    "live": {
        "application_name": "{{app_id}}"
    }
}
