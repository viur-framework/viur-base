{
    "default": {
        "admin": "4.0.22",
        "builds": {
            "admin": {
                "command": "viur install admin 4.0.22",
                "kind": "exec"
            },
            "npm": {
                "command": "build",
                "kind": "npm",
                "source": ""
            }
        },
        "core": "3.5.10",
        "distribution_folder": "./deploy",
        "format": "1.1.1",
        "sources_folder": "./sources",
        "vi": "not-in-use"
    },
    "develop": {
        "application_name": "{{app_id}}",
        "version": "dev-$(user)"
    },
    "live": {
        "application_name": "{{app_id}}",
        "version": "live-$(year)-$(month)-$(day)-$(hour)-$(minute)-$(second)"
    }
}